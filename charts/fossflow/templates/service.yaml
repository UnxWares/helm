apiVersion: v1
kind: Service
metadata:
  name: {{ include "fossflow.fullname" . }}
  labels:
    {{- include "fossflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fossflow.selectorLabels" . | nindent 4 }}
