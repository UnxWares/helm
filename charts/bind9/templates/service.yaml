{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bind9.fullname" . }}
  labels:
    {{- include "bind9.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: dns-tcp
      port: {{ .Values.service.ports.dnsTcp }}
      targetPort: dns-tcp
      protocol: TCP
      {{- if and .Values.service.ports.dnsTcpNodePort (ne .Values.service.type "ClusterIP") }}
      nodePort: {{ .Values.service.ports.dnsTcpNodePort }}
      {{- end }}

    - name: dns-udp
      port: {{ .Values.service.ports.dnsUdp }}
      targetPort: dns-udp
      protocol: UDP
      {{- if and .Values.service.ports.dnsUdpNodePort (ne .Values.service.type "ClusterIP") }}
      nodePort: {{ .Values.service.ports.dnsUdpNodePort }}
      {{- end }}

    - name: rndc
      port: {{ .Values.service.ports.rndc }}
      targetPort: rndc
      protocol: TCP
      {{- if and .Values.service.ports.rndcNodePort (ne .Values.service.type "ClusterIP") }}
      nodePort: {{ .Values.service.ports.rndcNodePort }}
      {{- end }}

  selector:
    {{- include "bind9.selectorLabels" . | nindent 4 }}
{{- end }}