apiVersion: v1
kind: Service
metadata:
  name: {{ include "laravel-generic.fullname" . }}
  labels:
    {{- include "laravel-generic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laravel-generic.selectorLabels" . | nindent 4 }}
